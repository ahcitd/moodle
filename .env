# ========================================
# การตั้งค่าฐานข้อมูล MariaDB
# ========================================

# รหัสผ่าน root ของฐานข้อมูล (ควรเปลี่ยนเป็นรหัสที่ปลอดภัย)
MYSQL_ROOT_PASSWORD=strongRootPassword123

# ชื่อฐานข้อมูลที่จะสร้างสำหรับ Moodle
MYSQL_DATABASE=moodle

# ชื่อผู้ใช้ฐานข้อมูลที่ Moodle จะใช้เชื่อมต่อ
MYSQL_USER=moodleuser

# รหัสผ่านของผู้ใช้ฐานข้อมูล (ควรเปลี่ยนเป็นรหัสที่ปลอดภัย)
MYSQL_PASSWORD=moodlePassword456


# ========================================
# การตั้งค่า Moodle
# ========================================

# ข้ามการติดตั้งอัตโนมัติหรือไม่ (no = ติดตั้งอัตโนมัติ, yes = ข้าม)
MOODLE_SKIP_BOOTSTRAP=no

# ชื่อผู้ใช้ผู้ดูแลระบบ Moodle (admin)
MOODLE_USERNAME=admin

# รหัสผ่านผู้ดูแลระบบ (ต้องมีความยาวอย่างน้อย 8 ตัวอักษร)
MOODLE_PASSWORD=Admin@123456

# อีเมลผู้ดูแลระบบ
MOODLE_EMAIL=admin@example.com

# ชื่อเว็บไซต์ Moodle ที่จะแสดง
MOODLE_SITE_NAME=ระบบ Moodle Learning

# ภาษาเริ่มต้น (th = ไทย, en = อังกฤษ, th_utf8 = ไทย UTF-8)
MOODLE_LANG=th

# URL หรือ IP สำหรับเข้าถึง Moodle (เช่น localhost, domain.com, หรือ IP address)
MOODLE_HOST=localhost

# ใช้ HTTPS หรือไม่ (yes = HTTPS, no = HTTP)
MOODLE_HTTPS=no


# ========================================
# การตั้งค่า PHP
# ========================================

# ขนาดไฟล์สูงสุดที่อัพโหลดได้ (หน่วยเป็น MB)
PHP_UPLOAD_MAX_FILESIZE=512M

# ขนาดข้อมูล POST สูงสุด (ควรมากกว่าหรือเท่ากับ UPLOAD_MAX_FILESIZE)
PHP_POST_MAX_SIZE=512M

# หน่วยความจำสูงสุดที่ PHP สามารถใช้ได้
PHP_MEMORY_LIMIT=512M


# ========================================
# พอร์ตสำหรับเข้าถึงเว็บไซต์
# ========================================

# พอร์ต HTTP สำหรับเข้าถึง Moodle (เช่น http://localhost:8080)
MOODLE_HTTP_PORT=8080

# พอร์ต HTTPS สำหรับเข้าถึง Moodle (เช่น https://localhost:8443)
MOODLE_HTTPS_PORT=8443
